# Example Dockerfile for local development
# Quick build without multi-platform support

FROM golang:1.24-alpine
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
# Replace with your actual build command
RUN go build -o app ./cmd/your-app
EXPOSE 8080
CMD ["./app"]
