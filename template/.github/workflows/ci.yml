# CI: single source of truth is scripts/verify (runs scripts/verify-steps.sh).
# Customize for your project's language and requirements.

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Example: Go projects - uncomment if needed
      # - name: Set up Go
      #   uses: actions/setup-go@v5
      #   with:
      #     go-version-file: go.mod

      # Example: Node projects - uncomment if needed
      # - name: Set up Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'

      # Example: Python projects - uncomment if needed
      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'

      - name: Verify (runs scripts/verify-steps.sh)
        run: ./scripts/verify
