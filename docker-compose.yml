# Example Docker Compose for the default app. Replace with your service.
# Usage: docker compose up -d

services:
  app:
    build: .
    image: app:latest
    command: ["./hello"]
    # ports:
    #   - "8080:8080"
    # environment:
    #   - APP_ENV=production
    restart: unless-stopped
