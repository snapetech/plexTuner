# Example Docker Compose. Replace with your service(s) for your stack.
# Usage: docker compose up -d

services:
  app:
    build: .
    image: app:latest
    # command: ["your-app"]
    # ports:
    #   - "8080:8080"
    # environment:
    #   - APP_ENV=production
    restart: unless-stopped
