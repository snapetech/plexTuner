**Latest (2026-05-12):** **Hardening Plex Live TV entitlement proxy.** User found the VPN/proxy path was working but asked whether it gives random users owner/admin access. Live kspls0 service was running `-elevate-all`; unauthenticated public requests to the media frontend returned `200`. Fix is implemented and deployed: inbound Plex token is required, non-owner tokens are validated against PMS before elevation, Live TV classification is narrower, and live service now runs `-elevate-live-tv -neutralize-owner-history`. `./scripts/verify` and `./scripts/release-readiness.sh` passed; live fake/no-token checks now return `401/403`. Next step: commit, tag, push release.
