**Latest (2026-05-12):** **Correcting v0.1.59 deployment wording.** User clarified that k3s should remain documented and supported for users/labs; the forbidden fallback is only our local production split-brain path where k3s/local jobs register the same Plex DVR identity as the systemd-owned host. Docs now list k3s as supported with ownership guardrails. `./scripts/verify` and `./scripts/release-readiness.sh` passed; next step is commit, tag `v0.1.60`, and push.
