# Plex Tuner config. Copy to .env and fill in. Never commit .env.
#   cp .env.example .env

# Indexing: use -m3u URL or -api -user -pass on the command line (or in docker command).
# Optional: use range downloads for large files (default: single GET)
# PLEX_TUNER_RANGE_DOWNLOAD=0
