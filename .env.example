# App config — template default. Copy to .env and fill in. Never commit .env.
#   cp .env.example .env

# --------------- Generic (replace with your app's vars) ---------------
# APP_ENV=development
# APP_DEBUG=false
# LOG_LEVEL=info
# SERVER_ADDR=:8080

# Add your own keys below. Keep secrets out of the repo.
